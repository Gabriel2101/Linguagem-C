#include <stdio.h>

int somatorio(int n){
    int i,aux;
    for (i=1;i<n;i++){
        aux+=5*(i*i)+2*i+8;
    }
    return aux;
}

int main()
{
    int soma,n;
    scanf("%d",&n);

    soma=somatorio(n);
    printf("%d",soma);
    return 0;
}
